# Design

## Visual Direction

차분하고 정보가 잘 읽히는 모바일 청첩장을 목표로 한다. 사진과 실제 예식 정보가 첫 번째 시각 신호여야 한다.

## Layout

- 첫 화면은 커버 이미지와 핵심 이름/날짜가 바로 보이게 구성한다.
- 페이지 섹션은 카드 안에 다시 카드를 넣지 않는다.
- 반복 항목, 모달, 폼 그룹에만 카드 스타일을 사용한다.
- 카드 radius는 8px 이하를 기본으로 한다.

## Typography

- 본문은 모바일에서 15px-17px 범위를 기본으로 한다.
- 긴 문구는 줄 간격을 충분히 주되, 과도한 히어로 타이포를 반복하지 않는다.
- viewport width에 따라 글자 크기를 직접 스케일하지 않는다.
- letter spacing은 기본 0으로 둔다.

## Color

- 단일 색상 계열만 반복하는 팔레트는 피한다.
- 기본 배경은 따뜻한 흰색 또는 아주 옅은 중립색으로 둔다.
- 강조색은 버튼, 링크, 선택 상태에만 제한적으로 사용한다.
- 경고/오류/성공 상태는 색만 의존하지 않고 텍스트도 함께 제공한다.

## Components

- 버튼에는 가능한 경우 `lucide-react` 아이콘을 사용한다.
- 숫자 입력은 `input[type=number]` 또는 stepper로 구현한다.
- 이진 설정은 checkbox 또는 toggle을 사용한다.
- 옵션 선택은 radio, segmented control, select 중 의미에 맞는 것을 사용한다.
- 낯선 아이콘에는 접근 가능한 라벨을 제공한다.

## Map

- 지도는 장식 카드가 아니라 `오시는 길` 섹션의 핵심 정보로 배치한다.
- 지도 위에 텍스트나 버튼을 겹치지 않는다.
- 지도 실패 대체 UI도 같은 높이와 정보 밀도를 유지한다.

## Accessibility

- 폼 라벨은 시각적으로 명확하거나 스크린리더용 라벨을 제공한다.
- 버튼과 링크는 키보드로 접근 가능해야 한다.
- 색 대비는 일반 텍스트 기준 WCAG AA 수준을 목표로 한다.
- 모달은 포커스 이동, 닫기 버튼, ESC 닫기를 제공한다.
